node_modules/
npm-debug.log*
ui-test-artifacts/
.DS_Store

# Local/runtime environment files — never commit secrets
.env
.env.*
*.env
*.env.*
!.env.example
!*.env.example

# Local Auth v2 runtime database — never commit account/session data
.data/

# Self-hosted runtime media mounts may be directories or symlinks.
public/soundboards
public/uploads

# Capacitor / Android Studio build artifacts
android/.gradle/
android/.idea/
android/.cxx/
android/build/
android/app/build/
android/app/intermediates/
android/app/generated/
android/app/debug.keystore
android/local.properties
