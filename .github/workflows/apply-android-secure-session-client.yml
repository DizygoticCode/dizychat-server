name: Apply Android Secure Session Client

on:
  push:
    branches:
      - feat/android-slice1-core
    paths:
      - .github/workflows/apply-android-secure-session-client.yml
      - scripts/ci/apply-android-secure-session-client.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply exact secure session client patch
        run: python3 scripts/ci/apply-android-secure-session-client.py
      - name: Commit patch when needed
        shell: bash
        run: |
          if git diff --quiet -- public/chat.js; then
            echo "No patch required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/chat.js
          git commit -m "feat: wire durable Android login sessions"
          git push origin HEAD:feat/android-slice1-core
